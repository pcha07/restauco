import { Link } from "react-scroll";

const Footer = () => {
  return (
    <footer className="w-full bg-orange-100 flex flex-col items-center justify-center">
      
    </footer>
  );
};

export default Footer;
